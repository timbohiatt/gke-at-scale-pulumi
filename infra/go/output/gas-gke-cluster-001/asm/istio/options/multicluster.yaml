apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
spec:
  values:
    global:
      multiCluster:
        # Provided to ensure a human readable name rather than a UUID.
        clusterName: "cn-thiatt-manual-pulumi-005-us-central1-gas-gke-cluster-001" # {"$ref":"#/definitions/io.k8s.cli.substitutions.cluster-name"}
