apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
spec:
  values:
    global:
      multiCluster:
        # Provided to ensure a human readable name rather than a UUID.
        clusterName: "cn-thiatt-manual-015-australia-southeast1-gas-gke-cluster" # {"$ref":"#/definitions/io.k8s.cli.substitutions.cluster-name"}
