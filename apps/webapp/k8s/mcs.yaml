---
apiVersion: networking.gke.io/v1
kind: MultiClusterService
metadata:
  name: demo-mcs
  namespace: demo
spec:
  template:
    spec:
      selector:
        app: demo
      ports:
      - name: web
        protocol: TCP
        port: 8080
        targetPort: 8080